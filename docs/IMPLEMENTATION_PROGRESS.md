# 房源租赁 Agent 系统改造进度

> 最后更新：2026-07-27  
> 总体方案：`docs/COMPLETE_OPTIMIZATION_PLAN.md`  
> 规则：只有完成实现并通过对应验收后，任务才标记为 `[x]`；仅完成分析、设计或部分编码不得标记为完成。

## 状态说明

- `[x]`：已完成并有验收证据。
- `[-]`：进行中，尚未达到退出标准。
- `[ ]`：尚未开始。
- `BLOCKED`：等待用户决策或外部条件。

## 已完成列表

### 改造准备

- [x] 完成当前系统问题诊断与整体优化方案。
  - 验收证据：`docs/COMPLETE_OPTIMIZATION_PLAN.md`。
  - 已识别问题：纯语义零召回会退化为任意房源；结构化条件下未使用向量候选且数据库无序 `LIMIT 50`；缺少检索评测集；向量索引缺少持续健康检查。
  - 注意事项：该项只是设计与诊断完成，不代表任何检索问题已经修复。
- [x] 验证改造前后端测试基线。
  - 验收证据：2026-07-26 执行 `backend\\mvnw.cmd test`，结果为 96 个测试全部通过。
  - 注意事项：这是代码回归基线，不是检索质量基线；尚无 Recall@K、Precision@K、零命中正确率和延迟报告。

## 已完成阶段：阶段 0 - 固定基线和修复当前检索红线

阶段状态：`已验收完成，当前进入阶段 1`

### 0.1 最小人工检索评测集

- [x] 确定评测集规模、数据来源和文件格式。
  - 验收证据：决策 D-001；当前数据库快照、150 条查询、JSONL。
- [x] 确定快照范围、数据处理、查询分布和标注方式。
  - 验收证据：决策 D-006 至 D-010。
  - 规则复核：`backend/src/test/resources/evaluation/stage0-v1/LABEL_REVIEW_V1.md`。
- [x] 建立版本化查询样本与相关房源标注。
  - 验收证据：`backend/src/test/resources/evaluation/stage0-v1/`；110 套房源、150 条查询、90 条语义标签已人工确认。
- [x] 覆盖纯结构化、纯语义、混合条件、零命中、冲突条件和脏数据场景。
  - 注意事项：向量库不可用属于运行时故障场景，将随离线执行器和 0.4 健康检查补充。
- [x] 实现可重复执行的离线评测程序。
  - 验收证据：`RetrievalMetrics.java`、`Stage0CurrentBaselineIT.java`、`baseline-application.yml`。
  - 测试证据：`RetrievalMetricsTest` 与 `Stage0EvaluationDatasetTest` 共 5 项通过；基线集成测试 1 项通过；完整后端回归 101 项通过。
- [x] 按 M3 + K1 输出 Recall@K、Precision@K、MRR/nDCG、硬条件违反率、零命中正确率和延迟。
  - 验收证据：`backend/src/test/resources/evaluation/stage0-v1/baselines/mysql-fallback/`。
  - 关键结果：宽松 Recall@20=0.6460，严格 Recall@20=0.6462，nDCG@20=0.5878，零命中正确率=50%，硬条件违规率=0%。
  - 关键问题：10 条语义零命中查询共错误返回 200 套房源，证明假命中问题可稳定复现。

当前数据检查：

- 当前数据库共 153 套房源，快照仅包含 110 套已审核且上架房源。
- 公开房源中有 72 套整租、38 套合租，覆盖 40 个区域组合。
- 20 套房源只关联到城市级“大连”，缺少区县和街道；按决策保留并添加数据质量标记。
- 110 套房源只有 50 种不同描述，重复文本可能限制语义评测的区分度。

注意事项：

- 评测集必须基于一份固定的房源数据快照，否则不同日期的指标不可比较。
- “相关房源”需要最终人工确认；不能用当前检索结果反向生成标准答案。
- 首批数据字段较少，通勤、家电等尚未建模的查询只能标记为当前阶段不支持，不能把它们计为模型召回失败。
- 用户已确认当前项目数据全部为虚构数据，因此本轮快照不做脱敏；未来接入真实数据时必须重新启用脱敏规则。
- 创建 0.1 基线时评测配置主动关闭了 Chroma。后续确认默认租户为空，但 Spring AI 实际使用 `SpringAiTenant/SpringAiDatabase/housing-rag`，其中有 447 条可查询向量。
- 0.3 已将统一检索内部上限调整为 50，Recall@50 已具备区分力；Agent/UI 的对外上限仍为 20。
- 本地热数据库延迟只适合后续同环境对比，不能作为生产 SLA。

### 0.2 修复纯语义零召回假命中

- [x] 为向量召回和统一检索定义可区分状态。
  - 向量层：`SUCCESS_WITH_RESULTS`、`SUCCESS_EMPTY`、`UNAVAILABLE`。
  - 业务层：`MATCHED`、`NO_MATCH`、`DEGRADED_STRUCTURED`、`RETRIEVAL_UNAVAILABLE`。
- [x] 按 E1 修复成功零命中查询任意公开房源的问题。
  - 行为：向量检索成功但没有 ID 时直接返回 `NO_MATCH`，不访问 MySQL 任意候选。
- [x] 按 U1 实现向量库故障降级。
  - 有硬条件：仅执行 MySQL 结构化筛选并标记 `DEGRADED_STRUCTURED`。
  - 无硬条件：返回 `RETRIEVAL_UNAVAILABLE`，不返回房源。
- [x] 将检索状态传播到 RAG、Agent 工具、普通响应、SSE、消息元数据、会话上下文和前端 Store。
  - 上下文修复：零命中或故障后清空上一轮候选，避免“第一套”错误引用旧房源。
- [x] 增加服务层和接口级回归测试。
  - 测试证据：相关定向测试 39 项通过；完整后端回归 108 项通过；前端生产构建通过。
- [x] 使用同一数据快照生成 0.2 后检查点。
  - 验收证据：`backend/src/test/resources/evaluation/stage0-v1/checkpoints/after-0.2/COMPARISON.md`。
  - 结果：零命中正确率由 50% 提升至 100%，错误返回房源数由 200 降至 0，硬条件违规率保持 0%。

注意事项：

- 0.2 验收报告使用 `MYSQL_FALLBACK_VECTOR_DISABLED` 隔离配置，因此只能评价降级路径，不能据此判断向量库是否存在或评价语义召回。
- 宽松 Recall@20 由 0.6460 降至 0.6125，是 50 条无硬条件查询不再用任意房源虚增召回的预期结果。
- `DEGRADED_STRUCTURED` 只表示满足可执行的结构化条件，回复不得宣称满足未验证的语义偏好。
- 数据库无序 `LIMIT 50` 和内部最多返回 20 套的问题已由 0.3 修复。

### 0.3 修复结构化条件下的无序候选截断

- [x] 确定结构化条件查询的候选生成与排序策略。
  - 验收证据：决策 D-003；采用 C1 + T1。
  - C1：结构化硬条件在 MySQL 中筛选全部公开房源，移除无序 `LIMIT 50`，再在服务层全局排序。
  - T1：统一检索内部最多返回 50 条，Agent/UI 现有对外上限仍为 20 条。
- [x] 移除无解释的 `LIMIT 50`，或替换为稳定且可解释的候选策略。
  - 实现证据：`AiMapper.searchHouses` 不再包含数据库层 `LIMIT 50`。
- [x] 保证硬条件在截断前完整执行。
  - 实现证据：MySQL 返回完整硬筛候选，`AiHouseSearchService` 全局排序后才截取内部前 50 条。
- [x] 增加超过 50 条候选时的回归测试。
  - 单元证据：60 条候选全部参与排序，最高分的第 60 条排到首位，再截取 50 条。
  - 集成证据：真实 MySQL 评测出现 56 条与 62 条硬筛候选；14 条查询返回超过 20 条，2 条返回 50 条。
- [x] 固化 0.3 后同数据快照检查点。
  - 验收证据：`backend/src/test/resources/evaluation/stage0-v1/checkpoints/after-0.3/COMPARISON.md`。
  - 关键结果：宽松 Recall@50 由 0.6125 提升至 0.6779，严格 Recall@50 由 0.6154 提升至 0.6784；零命中正确率 100%，硬条件违规率 0%。
  - 回归证据：定向服务测试 6 项、MySQL 集成评测 1 项、完整后端测试 109 项全部通过。

注意事项：

- 当前房源字段和索引有限，C1 取消上限后可能产生性能问题；阶段 1 扩充数据量时必须通过查询计划和压力测试复核索引。
- 若采用分页或扩大候选池，必须有稳定 `ORDER BY`，否则结果会随执行计划变化。
- 排序权重必须通过评测校准，不能在没有数据时宣称最优。
- 0.3 的 MySQL 降级检查点中向量分数为 0，大量候选依赖房源 ID 稳定打破同分；这不是最终相关性排序方案。

### 0.4 向量 Collection 健康检查

- [x] 纠正 Collection 命名空间诊断并核验现有索引。
  - 实际位置：`SpringAiTenant/SpringAiDatabase/housing-rag`，不是默认租户。
  - 改造前索引组成：440 条房源分片和 7 条 FAQ，共 447 条；512 维向量自查询 Top1 距离为 0。
  - 0.4 同步后新增 1 条 `index_state` 水位文档，当前共 448 条。
- [x] 修复本地 Embedding 模型启动路径和 ONNX 外部权重布局。
  - `.env` 使用 `../.runtime`；Docker 显式使用 `/app/.runtime`。
  - `setup-ai-model.ps1` 同时将外部权重放到项目根目录和 `backend` 工作目录。
- [x] 增加真实 BGE + Chroma 显式集成测试。
  - 验收证据：`ChromaSemanticRetrieverIT` 和 `checkpoints/after-0.3/chroma-smoke/`。
  - 结果：10/10 查询为 `SUCCESS_WITH_RESULTS`，Top10 查询命中率 100%，MRR=0.9000，平均 Recall@10=0.4421。
  - 安全证据：初次只读冒烟设置 `initialize-schema=false`，执行前后 Collection 均为 447 条；0.4 水位升级后在 448 条索引上复测，语义指标保持不变。
- [x] 确定健康检查暴露级别和失败语义。
  - 决策 D-005：应用可启动、liveness 保持正常；Collection 缺失、为空或过期时 readiness 不可用，业务继续按 U1 降级。
- [x] 检查 VectorStore Bean、Collection 可访问性、文档数量和最近同步状态。
  - `ragVector` 依次检查 VectorStore、正确命名空间下的 Collection、非空文档数、唯一水位文档、MySQL 公开房源数、最新更新时间和索引结构版本。
  - 每次全量同步开始先移除旧水位，440 条房源分片与 7 条 FAQ 全部成功后才写入新水位；中途失败不能继续宣称索引健康。
  - 真实验收：`ChromaIndexHealthIT` 从 MySQL 同步 110 套房源，Collection 为 448 条且组件状态为 `UP`。
- [x] 增加健康状态测试与运维说明。
  - liveness 仅检查进程；readiness 包含 `ragVector`，向量功能关闭时分别返回 200/503。
  - 单元测试覆盖未启用、Collection 缺失、空库、健康、同数量内容更新和 Chroma 异常 6 类场景。
  - 运维文档：`docs/operations/RAG_HEALTH_CHECK.md`；健康结果缓存 10 秒，匿名接口不暴露组件详情。

注意事项：

- 不能仅以 HTTP 可连接作为健康；空 Collection、索引陈旧同样会导致业务不可用。
- 健康接口不得泄露连接凭据、内部地址或房源敏感元数据。
- 已选择 H1：向量异常不阻止进程启动，但必须影响 readiness，不能只记录日志。
- Spring AI 2.0 的 `ChromaApi.getEmbeddings` 无法读取当前 Chroma 1.0.0 `/get` 返回的额外字段，会触发 `StreamReadException`；健康检查改用带 `type=index_state` 过滤的真实向量查询，同时验证 Embedding 与查询链路。
- 水位方案是阶段 0 的全量同步保护；阶段 3 引入 Outbox 后仍需增加事件积压、失败重试和逐房源版本指标。

### 0.5 基线报告与阶段验收

- [x] 记录改造前 MySQL 降级检索指标和隔离运行配置。
  - 验收证据：`backend/src/test/resources/evaluation/stage0-v1/baselines/mysql-fallback/`。
- [x] 使用同一快照完成真实 BGE + Chroma 端到端冒烟验收。
  - 验收证据：`backend/src/test/resources/evaluation/stage0-v1/checkpoints/after-0.3/chroma-smoke/REPORT.md`。
  - 注意：这是 10 条代表性语义查询的链路验收，不是最终推荐质量基线。
- [ ] 房源数据模型重构后，基于新字段和最终查询目标重建完整向量检索评测集。
  - 顺延原因：当前阶段尚无通勤、电梯、楼层、费用、起租时间和家电明细等目标字段；在旧字段上扩充这些标签没有真实性。
  - 进入时点：阶段 1 数据模型完成后执行，阶段 2 Elasticsearch 切换后再次校准。
- [x] 完成 0.2 后同数据快照检查点并与改造前对比。
  - 验收证据：`backend/src/test/resources/evaluation/stage0-v1/checkpoints/after-0.2/`。
- [x] 阶段 0 全部修复后使用同一数据快照重新评测。
  - MySQL 隔离评测：145 条可执行查询；宽松/严格 Recall@50=0.6779/0.6784；零命中正确率 100%；硬条件违规率 0%；向量启用率 0%。
  - 真实向量校准：40 条正常语义查询和 10 条人工确认语义零命中查询，扫描 0.50 至 0.65 六个阈值。
- [x] 验证零命中不产生假推荐。
  - 问题复现：阈值 0.50 时纯语义零命中仅 4/10 返回空，6 条产生假推荐，误命中最高分 0.5752。
  - 修复结果：默认阈值调整为可配置的 0.58；10/10 语义零命中返回 `SUCCESS_EMPTY`，正常语义 Top10 命中率保持 100%，全量语义 MRR 保持 0.9219。
- [x] 验证现有功能回归测试通过。
  - 默认后端 117 项全部通过；真实正常语义/零命中 2 项、阈值校准 1 项、真实索引健康 1 项和 MySQL 最终评测 1 项显式集成测试通过。
  - 真实运行态 `liveness` 与 `readiness` 均为 HTTP 200 / `UP`。
- [x] 形成阶段 0 验收报告并记录遗留问题。
  - 验收证据：`backend/src/test/resources/evaluation/stage0-v1/checkpoints/stage0-final/REPORT.md`。
  - 结论：阶段 0 验收通过，可进入阶段 1；当前指标不代表稀疏旧字段已经达到最终推荐质量。

注意事项：

- 报告必须记录数据快照、Embedding 模型、阈值、TopK、代码版本和运行环境。
- 当前目录未检测到可用 Git 仓库时，应使用报告编号或文件哈希固定版本证据。
- 阶段 0 完成标准是问题可重复、可量化且假命中消失，不要求在本阶段达到最终推荐质量。
- 0.58 阈值对当前固定快照有效，存在过拟合风险；阶段 1/2 必须使用新房源字段和新查询重新标注、重建索引并校准。
- MySQL 降级评测必须通过 `@SpringBootTest(properties=...)` 高优先级关闭 AI/Chroma，不能只依赖可能被 `.env` 覆盖的 YAML。

## 当前阶段：阶段 1 - 房源数据模型和传统表单

当前状态：`1.2 数据库模型逐表商定中；house_location 已确认，下一步 house_lease_term`

### 1.1 现状盘点与兼容边界

- [x] 盘点真实 MySQL `house`、`area` 及房源关联外键。
  - 验收证据：`docs/阶段1现状盘点与兼容边界.md`。
  - 数据证据：153 套房源、110 套公开房源；公开房源中 84 套卫生间数为空、92 套厨房数为空、20 套只关联顶级区域。
- [x] 盘点后端发布、编辑、审核、详情、删除、付费、通知和 OSS 清理链路。
  - 兼容边界：发布资格与主数据写入必须保持事务一致；房东编辑后重新审核；拒绝审核退还发布付费；公开详情继续隐藏精确地址和手机号。
- [x] 盘点前端发布、编辑、详情、个人房源和管理审核页面。
  - 结论：当前只支持基础字段和单图；发布与编辑存在重复实现；管理审核无法检查字段来源、未知项和冲突。
- [x] 核对真实库、Java 类型和 H2 测试结构的一致性。
  - 发现 1：真实 `area.level` 为 `province/district/street` 字符串，Java/测试/前端使用整数；真实接口已验证为返回空列表或 HTTP 500。
  - 发现 2：真实库七类房源外键均为 `NO ACTION`，H2 测试使用级联或置空；当前物理删除可能在真实库失败。
  - 注意事项：本项只完成只读盘点与运行验证，尚未修改数据库、接口或页面。

### 1.2 数据库模型方案

- [-] 按依赖顺序与用户逐表商定最终模型。
  - 已确认：`house` 保持聚合根；保留现有兼容字段；新增高频结构化事实、版本、完整度和软删除字段。
  - 用户调整：阳台只保存 `has_balcony`，不保存数量；不保存建筑年份；新版发布必须明确有无阳台。
  - 决策证据：`docs/阶段1数据库设计决策.md`。
  - 已确认：`house_location` 一对一位置表；小区必填，楼栋保存，单元号与房间号可选，精确地址和坐标受权限保护；阶段 1 坐标可空，地图阶段后审核要求定位成功。
  - 下一张表：`house_lease_term`。
  - 停止边界：全部相关表和迁移方案确认前，不创建新表、不修改真实表、不生成可执行迁移。

## 后续阶段

- [-] 阶段 1：房源数据模型和传统表单。
- [ ] 阶段 2：Elasticsearch 双轨迁移。
- [ ] 阶段 3：Outbox 增量同步。
- [ ] 阶段 4：地图和通勤服务。
- [ ] 阶段 5：上下文与任务系统。
- [ ] 阶段 6：房源发布 Agent。
- [ ] 阶段 7：租房决策 Agent。
- [ ] 阶段 8：知识、观测和上线验收。

总体注意事项：

- 不跨阶段提前引入 Agent 编排；先保证数据、传统接口和检索链路正确。
- 每个阶段都必须保留回滚路径，并在进入下一阶段前达到退出标准。
- 遇到问题必须先定位和记录原因；一般技术取舍由实现者基于证据决策，只有破坏性、不可逆或会改变产品目标的操作再请求用户确认。
- 测试、安全、可观测性和文档随每个阶段同步完成，不留到最后集中补齐。

## 决策记录

| 编号 | 决策主题 | 状态 | 用户选择 | 日期 | 影响范围 |
|---|---|---|---|---|---|
| D-001 | 首批评测集规模与组织方式 | 已确认 | 当前数据库快照；150 条查询；JSONL | 2026-07-27 | 0.1、0.5 |
| D-002 | 纯语义零召回的产品行为 | 已确认 | E1：正常零命中返回空结果和 `NO_MATCH`，提示调整条件，不自动放宽 | 2026-07-27 | 0.2 |
| D-003 | 结构化条件的候选与排序策略 | 已确认 | C1：完整硬筛后全局排序；T1：内部最多 50 条、Agent/UI 最多 20 条 | 2026-07-27 | 0.3、0.5 |
| D-004 | 向量库故障时的降级策略 | 已确认 | U1：有硬条件走 MySQL 并标记 `DEGRADED_STRUCTURED`；纯语义返回 `RETRIEVAL_UNAVAILABLE` | 2026-07-27 | 0.2、0.4 |
| D-005 | 健康检查是否影响应用启动/就绪状态 | 已确认 | H1：不阻止启动；liveness 正常；索引异常使 readiness 不可用；业务按 U1 降级 | 2026-07-27 | 0.4 |
| D-006 | 快照范围和数据处理 | 已确认 | 仅 110 套公开房源；数据为虚构数据，不做脱敏 | 2026-07-27 | 0.1 |
| D-007 | 150 条查询类别分布 | 已确认 | Q1：45 结构化、40 语义、40 混合、20 零命中、5 冲突或脏数据 | 2026-07-27 | 0.1 |
| D-008 | 相关性标注 | 已确认 | R2：0/1/2 三级标注，AI 生成初稿，用户最终确认歧义项 | 2026-07-27 | 0.1、0.5 |
| D-009 | 区域层级不完整数据 | 已确认 | 保留 20 条记录并增加数据质量标记 | 2026-07-27 | 0.1 |
| D-010 | 语义标签人工复核粒度 | 已确认 | V1：按 10 个语义规则组和 10 条语义零命中审核 | 2026-07-27 | 0.1 |
| D-011 | 组内问法不完全同义 | 已确认 | F2：每组保留 4 条查询并重写为严格同义表达 | 2026-07-27 | 0.1 |
| D-012 | 海景弱相关评分 | 已确认 | L2：普通景观、视野和夜景没有海景证据时由 1 分改为 0 分 | 2026-07-27 | 0.1 |
| D-013 | 语义零命中标签 | 已确认 | Z1：10 条特殊设施查询均应返回空结果 | 2026-07-27 | 0.1 |
| D-014 | 离线指标口径与 K 值 | 已确认 | M3：宽松/严格双口径加分级 nDCG；K1：Recall@10/20/50、Precision@5/10、nDCG@10/20、MRR | 2026-07-27 | 0.1、0.5 |
| D-015 | 空 Chroma 的首次索引方式 | 已解决 | 实际索引位于 Spring AI 租户且已有 447 条向量；0.3 真实冒烟复用现有索引，不因错误租户诊断而重建 | 2026-07-27 | 0.4、0.5 |
| D-016 | 索引新鲜度的阶段 0 判定方式 | 已实施 | 全量同步写入 `index_state`；readiness 比较 Collection 数量、公开房源数、源库最新更新时间和结构版本；索引升级后共 448 条 | 2026-07-27 | 0.4、0.5 |
| D-017 | 当前 BGE/Chroma 相似度阈值 | 已实施 | 选择可配置默认值 0.58；语义零命中正确率 100%，正常语义 Top10 命中率和 MRR 不下降；阶段 1/2 必须重校准 | 2026-07-27 | 0.5、1、2 |
| D-018 | MySQL 降级评测隔离方式 | 已实施 | 测试注解使用高优先级属性强制关闭 chat、embedding、vector store 和 Chroma，不依赖本机 `.env` | 2026-07-27 | 0.5 |
| D-019 | 阶段 1 `house` 主表边界 | 已确认 | 保留现有兼容字段；高频稳定单值留在主表；新增楼层、电梯、阳台布尔值、朝向、装修、供暖、版本、完整度与软删除；不保存阳台数量和建筑年份；新版发布有无阳台必填 | 2026-07-27 | 1.2 |
| D-020 | 阶段 1 `house_location` 位置表 | 已确认 | 小区名称必填；保存楼栋号，可选保存单元号和房间号；楼层与总层数归属 `house`；阶段 1 坐标可空，地图阶段后新版审核要求定位成功；精确地址和坐标不公开 | 2026-07-28 | 1.2、4 |

## 更新日志

### 2026-07-27

- 创建改造进度台账。
- 录入已完成的诊断、总体方案和 96 项测试基线。
- 将阶段 0 拆分为可独立验收的五部分，并记录首批待决策事项。
- 记录 D-001：评测数据采用当前数据库快照，首批 150 条查询，使用 JSONL 保存。
- 记录 D-006 至 D-009：仅公开房源、不脱敏、Q1 查询分布、三级相关性标注、保留区域层级不完整数据。
- 记录 D-010：采用 V1 规则级语义标签复核，并生成 `LABEL_REVIEW_V1.md`。
- 记录 D-011：执行 F2 同义化改写，重建查询集和哈希，数据校验测试 3 项通过。
- 记录 D-012/D-013：执行 L2/Z1，90 条标签改为 `HUMAN_CONFIRMED`，重建后校验测试 3 项通过。
- 记录 D-014：采用 M3 + K1 指标口径；完成离线指标实现与单元测试。
- 固化改造前 MySQL 降级基线：145 条查询执行成功，零命中正确率为 50%，10 条语义零命中查询错误返回 200 套房源。
- 完成 0.1 最终回归：执行 `backend\mvnw.cmd test`，101 项测试全部通过。
- 记录 D-002/D-004：采用 E1 + U1，区分正常零命中、结构化降级和检索不可用。
- 完成 0.2 全链路实现：状态贯穿 RAG、Agent、普通响应、SSE、消息元数据、上下文和前端 Store。
- 完成 0.2 验收：定向测试 39 项、完整后端测试 108 项和前端生产构建通过。
- 固化 0.2 后检查点：零命中正确率 100%，错误返回房源数 0；记录 Recall 下降的真实原因。
- 记录 D-003：采用 C1 + T1，开始实施完整硬筛、全局排序和内部 Recall@50 支持。
- 完成 0.3：移除 Mapper 无序 `LIMIT 50`，内部上限调整为 50，Agent/UI 上限保持 20。
- 固化 0.3 后检查点：14 条查询返回超过 20 条，宽松/严格 Recall@50 分别提升至 0.6779/0.6784。
- 完成 0.3 验收：60 条候选单元回归、145 条查询 MySQL 集成评测和完整后端 109 项测试通过。
- 纠正 Chroma 诊断：默认租户为空，但 Spring AI 租户中的 `housing-rag` 已有 447 条向量；D-015 无需重建。
- 真实链路测试定位并修复两个启动问题：`.env` 模型相对路径错误，以及 ONNX 外部权重未放在 `backend` 工作目录。
- 完成真实 BGE + Chroma 冒烟验收：10/10 查询成功且 Top10 命中人工相关房源，MRR=0.9000。
- 固化 Chroma 冒烟报告；模型安装脚本幂等、Docker 路径覆盖和默认后端 109 项回归均通过。
- 完成 0.4：新增 `ragVector` readiness、liveness/readiness 探针分组、索引水位文档、MySQL 更新时间水位查询和 10 秒健康缓存。
- 定位 Spring AI 2.0 `ChromaApi.getEmbeddings` 与 Chroma 1.0.0 `/get` 响应不兼容；改用真实向量查询读取水位后验收通过。
- 执行真实全量同步：110 套房源生成 440 条房源分片、7 条 FAQ 和 1 条水位文档，Collection 从 447 升级为 448，readiness 状态为 `UP`。
- 0.4 验收：健康判定单元测试 6 项、探针集成测试 2 项、真实同步健康测试 1 项通过；完整后端 116 项回归通过。
- 在 448 条索引上复测真实语义召回：10/10 查询成功，Top10 命中率 100%，MRR=0.9000，平均 Recall@10=0.4421。
- 新增 `docs/operations/RAG_HEALTH_CHECK.md`，记录探针语义、原因码、同步流程和阶段 3 遗留事项。
- 真实 Maven 启动发现 `spring-boot:run` 工作目录仍指向项目根目录，导致 `../.runtime` 模型路径失效；已统一为 `backend`，随后 8080 启动成功。
- 阶段 0.5 首次真实语义零命中测试仅 4/10 正确，证明 0.50 阈值仍会制造假推荐；完成 40+10 查询的六档阈值扫描。
- 记录 D-017：默认相似度阈值改为可配置的 0.58；语义零命中提升到 10/10，正常语义 Top10 命中率保持 100%。
- 最终 MySQL 评测首次误加载真实向量，定位为隔离配置可被本地环境覆盖；记录 D-018 并使用高优先级测试属性修复。
- 完成阶段 0 最终验收：MySQL 145 条查询、真实 Chroma 正常语义/零命中、117 项默认回归和运行态 liveness/readiness 全部通过。
- 固化 `stage0-final` 检查点及 SHA-256，形成 `STAGE0-FINAL-20260727` 验收报告；阶段 0 结束，下一步进入阶段 1。
- 原台账已移动到 `docs/.progress-history/IMPLEMENTATION_PROGRESS.before-D001.20260727.md`，当前文件为重建后的唯一活动台账。
- 完成阶段 1.1 只读现状盘点，新增 `docs/阶段1现状盘点与兼容边界.md`。
- 确认真实 `area.level` 与 Java/H2/前端的类型漂移，并通过运行接口复现地区选择链路异常。
- 确认真实房源关联外键删除规则与 H2 测试不一致，记录当前物理删除的生产风险。
- 按用户要求停止在新数据库表设计入口，未修改任何数据库结构，等待商定阶段 1.2 最终方案。
- 记录 D-019：确认 `house` 主表设计；新增 `docs/阶段1数据库设计决策.md`，下一步逐表商定 `house_location`。
- 记录 D-020：确认 `house_location` 一对一位置表，下一步逐表商定 `house_lease_term`。
